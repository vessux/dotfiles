# Beads Configuration File
# This file configures default behavior for all bd commands in this repository
# All settings can also be set via environment variables (BD_* prefix)
# or overridden with command-line flags

# Issue prefix for this repository (used by bd init)
# If not set, bd init will auto-detect from directory name
# Example: issue-prefix: "myproject" creates issues like "myproject-1", "myproject-2", etc.
# issue-prefix: ""

# Use no-db mode: JSONL-only, no Dolt database
# When true, bd will use .beads/issues.jsonl as the source of truth
# no-db: false

# Enable JSON output by default
# json: false

# Feedback title formatting for mutating commands (create/update/close/dep/edit)
# 0 = hide titles, N > 0 = truncate to N characters
# output:
#   title-length: 255

# Default actor for audit trails (overridden by BEADS_ACTOR or --actor)
# actor: ""

# Export events (audit trail) to .beads/events.jsonl on each flush/sync
# When enabled, new events are appended incrementally using a high-water mark.
# Use 'bd export --events' to trigger manually regardless of this setting.
# events-export: false

# Multi-repo configuration (experimental - bd-307)
# Allows hydrating from multiple repositories and routing writes to the correct database
# repos:
#   primary: "."  # Primary repo (where this database lives)
#   additional:   # Additional repos to hydrate from (read-only)
#     - ~/beads-planning  # Personal planning repo
#     - ~/work-planning   # Work planning repo

# JSONL backup (periodic export for off-machine recovery)
# Auto-enabled when a git remote exists. Override explicitly:
# backup:
#   enabled: false     # Disable auto-backup entirely
#   interval: 15m      # Minimum time between auto-exports
#   git-push: false    # Disable git push (export locally only)
#   git-repo: ""       # Separate git repo for backups (default: project repo)

# Integration settings (access with 'bd config get/set')
# Non-secret keys (stored in the database):
# - jira.url, jira.project
# - linear.team_id
# - github.org, github.repo
#
# Secret keys (stored in this file but prefer env vars to avoid git exposure):
# - linear.api_key  → use LINEAR_API_KEY env var instead
# - github.token    → use GITHUB_TOKEN env var instead

sync.remote: "git+https://github.com/vessux/dotfiles.git"

export.git-add: false

# Auto-push Dolt (refs/dolt/data) to sync.remote after writes. OFF: in embedded mode it is
# best-effort/racy — a short-lived `bd` process can exit before the push's network round-trip,
# so identical ops disagree and some captures never reach the remote (dotfiles-sp0). The push
# side now rides the `bd` shim (~/.config/bin/bd) — a debounced, crash-safe background
# coordinator that flushes after every mutating `bd` — per ADR 0013. Leaving auto-push on would
# just add a redundant, racy double-push. Pulls stay explicit (`bd dolt pull`).
dolt.auto-push: false

# Dolt database name — the tracked, host-independent record of which database on
# the shared dolt sql-server belongs to this repo. metadata.json also carries it,
# but that file is host-local (gitignored, see .beads/.gitignore), so this is the
# copy that survives a fresh clone and tells provisioning/bootstrap the right name
# instead of leaving it to bd's directory-basename auto-detection.
#
# Note: bd only reads this file once metadata.json has established the workspace —
# config.yaml alone does NOT make a checkout usable. Run `bd bootstrap` on a fresh
# clone; teleport's beads_fetch already does this for a new host.
#
# Host-specific connection settings (dolt.shared-server / host / port) are
# deliberately NOT set here: they would follow the repo to the Mac, which stays
# embedded. bin/bd derives them per-project from metadata.json instead.
dolt.database: "dotfiles"