#!/usr/bin/env bash
set -euo pipefail

HERE="$(cd "$(dirname "$0")" && pwd)"
SEED="$HERE/seed.md"

escape_for_json() {
  local s="$1"
  s="${s//\\/\\\\}"
  s="${s//\"/\\\"}"
  s="${s//$'\n'/\\n}"
  s="${s//$'\r'/\\r}"
  s="${s//$'\t'/\\t}"
  printf '%s' "$s"
}

start_glean() {
  if command -v clerk >/dev/null 2>&1; then
    (clerk glean --async >/dev/null 2>&1 || true) &
  fi
}

start_glean

content="$(cat "$SEED")"
context="<clerk-operating-rules>
These are the Clerk workflow rules for this session. Follow them as procedure; explicit user and project instructions still win.

$content
</clerk-operating-rules>"
escaped="$(escape_for_json "$context")"
printf '{\n  "hookSpecificOutput": {\n    "hookEventName": "SessionStart",\n    "additionalContext": "%s"\n  }\n}\n' "$escaped"
