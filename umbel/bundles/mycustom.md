---
name: mycustom
description: borklog + karpathy-guidelines + grill-me + plannotator + tuidriver MCP
extends: [plannotator]
skills:
  - local/borklog
  - local/karpathy-guidelines
  - pocock/grill-me
mcps:
  - local/tuidriver
---

# mycustom

Project bundle: the `borklog` backlog/worklog skill, the
`karpathy-guidelines` coding-discipline skill, and `pocock/grill-me`,
plus the `tuidriver` MCP. Extends the `plannotator` bundle for its
`annotate` + `last` review skills.

Prereqs: `tuidriver` and `plannotator` binaries on `PATH`.
